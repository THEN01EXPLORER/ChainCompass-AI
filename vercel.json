{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "Backend API URL",
      "example": "https://chaincompass-api.onrender.com"
    }
  }
}
